# Feature module documentation

Each feature directory owns its contracts, adapters, presentation surface, tests, checklist and error range. The initial repository contains the complete module map; implementation status is tracked in `FEATURE-MAP.json` and `CHECKLIST.md`.
